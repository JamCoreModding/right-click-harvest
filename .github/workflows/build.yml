name: build and test

on:
  - push
  - pull_request

jobs:
  build:
    uses: JamCoreModding/.github/.github/workflows/mod.yml@main
    with:
      java-version: 21
      gametest-project-directory: fabric-gametest
